name: CI

on:
  pull_request:
    branches:
      - "**"
  push:
    branches:
      - master
  workflow_call:

jobs:
  checks:
    name: Run Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Placeholder for future checks
        run: echo "No checks defined yet."
